requests==2.32.3
python-dotenv==1.0.1
numpy>=1.26,<3
# PostgreSQL time-series storage (DigitalOcean Managed Postgres)
# psycopg3 avec wheel binaire (libpq embarquée, pas de compil requise)
psycopg[binary]>=3.1,<3.3
